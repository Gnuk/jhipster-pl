mixin jhlite-nav-item(options)
  -const {current, href} = options || {};
  if current
    span.jhlite-nav-item.-current(class='-current')
      block
  else
    a.jhlite-nav-item(href=`${href || '#'}`)
      block
